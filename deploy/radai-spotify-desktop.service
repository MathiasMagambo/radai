[Unit]
Description=Spotify desktop bridge for Radai
After=graphical-session.target

[Service]
Type=simple
Environment=DISPLAY=:0
ExecStart=/usr/bin/spotify --remote-debugging-port=9223 --remote-allow-origins=*
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
